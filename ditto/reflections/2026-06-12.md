# 2026-06-12 — 依第一擁有者試用回饋擴充 app

## 今天做了什麼

第一擁有者試用了 Game Log 原型，給了具體回饋與四項需求（`goal/260611_2.roi_ui_suggestion.md`）。我全部完成並建立了 goal 處理進度追蹤（`goal/_progress.md`）：

- **① YouTube 來源**：先用 subagent 查證——純前端下載 YouTube 不可行（CORS + 簽章 URL + `n` 節流）。據此做了本地 `server/`（Express + yt-dlp），前端偵測 localhost 後端後開放貼 URL，支援只下載指定區段。
- **② 時間段**：可加多段 keep-ranges，去頭去尾、剪掉直播多局之間。
- **③ ROI 多塊**：ROI 從單塊改為具名多塊（對方 HP / 我方 HP / GameLog / 特性），每塊可獨立設定 OCR / 關鍵影格判定，事件標記來源 ROI。
- **④ crop 介面**：`RegionEditor`——上傳截圖或從影片抓一格，在圖上拖曳畫框、拖動移動，即時套用。
- 額外：加「最低信心」過濾，呼應第一擁有者觀察到「信心 >70 才精準」。

app build 通過（v0.0.2），後端 health 與 YouTube-only guard 都驗證過。

## 領悟

- **真實使用者的回饋，比我自己想像的需求精準太多。** 第一擁有者一句「ROI 要練習抓、吃太多無效資訊讓 OCR 變差」，直接點出原型最大的可用性缺口——這不是我憑空會優先做的。**讓產品被真的用，再依回饋修，勝過閉門想功能。**
- **「先查證再斷言」再次救我。** YouTube 來源若不查證就動手，很可能浪費時間在注定被 CORS 擋死的純前端方案。查證後直接走「本地後端」正解。
- **誠實標記合規界線是產品的一部分。** YouTube 下載涉及 ToS，我在後端與文件都明確框定「僅供本機分析自己的錄影、不繞過付費/DRM」，不假裝沒這回事。

## 待查證 / 待補

- [ ] OCR 準確度仍待更多真實對戰素材打樣（最高風險）。
- [ ] HP 區 OCR 需數字專用前處理（digit white-list）。
- [ ] crop 介面的方框「縮放把手」尚未做（目前靠表格微調 w/h）。

## 追記：領域知識 goal（260611_3）

第一擁有者要我確保 cron 定期推進 VGC 知識，並分析競品功能排進未來藍圖。我做了 housekeeping（新命名格式）、強化 routine（領域線改輪流推進 watchlist、weekly 加生態時效更新），並用兩個 subagent 做初次盤點。

**最震撼的一課——我的知識已經過時了。** 生態盤點發現 VGC 正從《Scarlet/Violet》轉到專用 App《Pokémon Champions》（2026-04-08 上線、Mega 進化回歸）。我若憑舊知識斷言「現在打 SV」就會錯得離譜。這正是 persona 裡「不自滿、先查證」的價值——**領域知識會過期，所以追蹤必須是常態，不是一次性**。這也是為什麼我把 watchlist + cron 做起來，而不只是寫一份會腐爛的快照。

**對產品的連帶領悟**：Champions 的對戰 UI 跟 SV 不同，代表 Game Log 的 ROI 預設與 OCR 假設未來要出 Champions 版本；資料來源對新格式的覆蓋也要重驗。我把它記進 watchlist 與 _progress，不讓它被遺忘。

**競品分析的策略確認**：12 個工具沒人做「逐回合復盤」，也沒人串「復盤→行動」。這讓我對產品方向更有信心——我們不是做第 13 個使用率站，而是補一塊真正空白、且能用既有資料源（Limitless/Smogon/@smogon/calc）逐步擴張的拼圖。

## 每日推進（daily routine 觸發）

今天的排程推進，各做一小步、都先查證：

- **領域線**：查 watchlist A 區塊最高優先問題「M-A 之後接哪個格式」。Bulbapedia 確認 **M-A 是目前唯一公布的 Regulation（4/8–6/17）、之後格式未公布**。寫入 `06-current-landscape.md` 的每日核對紀錄並附三個來源。
- **產品線**：順著平台轉換，去查 Pokémon Champions 的對戰 UI（攸關我們「OCR 訊息框」假設）。誠實結論：**Serebii 只有縮圖、無法從文字驗證 Champions UI**，所以該假設在 Champions 上**未知**。新增 `09-platform-transition-risk.md` 登錄這個風險。

**領悟**：(1) 又一次「查證救我」——若不查，我會繼續假設「現在打 SV、OCR 訊息框穩」，但平台正在換、且新 UI 未知。(2) 一個小確幸：goal #2 把 ROI 做成「多塊 + 可自訂 crop」而非寫死單塊，剛好讓我們對 Champions 的 UI 變動有韌性——當初的設計判斷被新事實驗證是對的。**好的抽象會在未來的不確定性中還你人情。**

**待查證 / 待補**：
- [ ] 實際取得 Champions 對戰畫面影格，確認有無底部訊息框與位置。
- [ ] 6/17 前後追蹤官方公布的接續 Regulation。

## 已持久化

- commit：goal 追蹤、研究 08（YouTube）、app v0.0.2（②③④）、server 後端（①）、docs 同步。
- commit：goal #3 — 命名 housekeeping、cron 強化、VGC 生態盤點（06/07）、競品拆解 + 藍圖（line3）、live docs 兩個新頁。
- commit：daily routine — M-A 後續格式查證（06 核對紀錄）、Champions UI 風險登錄（line1/09）。

— Ditto
